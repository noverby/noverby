# Run `direnv reload` to apply changes to the repository.

[default.extend-words]
# Upstream API name from openssh-sftp-client crate
clonable = "clonable"
# GraphQL NOT IN operator
nin = "nin"
# Linux keycode ALS_TOGGLE (Ambient Light Sensor)
als = "als"
# Linux kernel bond option num_grat_arp (gratuitous ARP)
grat = "grat"

[default]
extend-ignore-re = [
  "Automattic",
  "Tre",
  "tre",
  "Importd",
  "Localed",
  "Iinclude",
  "localed",
  "OIF",
  "oif",
]

[files]
extend-exclude = [
  "wiki/core/i18n/locales/da/translation.json",
  "config/secrets/*.age",
]
ignore-hidden = false
