# Run `direnv reload` to apply changes to the repository.

[default.extend-words]
# Upstream API name from openssh-sftp-client crate
clonable = "clonable"

[default]
extend-ignore-re = [
  "Automattic",
  "Tre",
  "tre",
  "Importd",
  "Localed",
  "Iinclude",
  "localed",
  "OIF",
  "oif",
]

[files]
extend-exclude = [
  "wiki/core/i18n/locales/da/translations.json",
  "config/secrets/*.age",
]
ignore-hidden = false
