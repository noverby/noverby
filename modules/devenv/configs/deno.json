{
	"$comment": "After editing config files in this directory, run `direnv reload` to apply changes to the repository.",
	"lint": {
		"rules": {
			"tags": ["recommended"],
			"exclude": ["no-process-global", "no-window", "no-sloppy-imports"]
		}
	}
}
